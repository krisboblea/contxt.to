// Minimal auth placeholder — full NextAuth will be added in Phase 2
// For now, all operations work without authentication

export async function auth() {
  return null
}

export async function signIn() {}
export async function signOut() {}
